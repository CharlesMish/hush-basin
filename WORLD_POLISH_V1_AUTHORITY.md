# World Polish v1 — owner-authorized successor

The owner's approved September 5, 2026 implementation plan controls this
isolated successor of commit 3b245da432b5a20a1606b3a9b2d9559ba409db39.
It supersedes root M5 and nested R7 geometry, presentation, and world-identity
freezes only for three connected yards, honest matching render/collision cuts,
terrain transitions, architectural detail, palette, sky/light, map and spatial
classification, successor verification and delivery. Historical authority and
evidence stay historical; this build does not claim the old geometry gates.

Keep all craft, movement, tuning, collider, probes, input, camera behavior and
Run v0 scripts byte-identical. Preserve route bakes, destination positions,
outer footprint and the South Hop/detour fixture. New shortcuts are intended.
Use exact installed Godot 4.7.1.stable.official.a13da4feb and native Forward+.
No external packages, assets, services, custom shaders, new particles/audio,
gameplay systems, publication or account changes are authorized.

The source configuration is game/world/world_polish_v1.json. The standard-library
offline generator reads frozen source artifacts and writes a versioned overlay;
the old generated artifacts remain byte-identical. Geometry is clipped from
the original top triangles, extruded once, and consumed by both rendering and
collision. Protected foundations are excluded from every cut.

Historical visual pixel and ray expectations intersecting changed geometry or
lighting are replaced by structural visibility parity and a matched native
survey. Unchanged craft fixtures still require exact same-engine C1 traces.
Owner comfort, preference and freedom gates remain untested until owner play.
After the first complete verification allow at most two bounded repair rounds.

Bounded repair 1 corrects verification/version binding: the old C1 recorder
allows startup physics before its reset and samples at the pre-physics signal.
The retained old trace differed only in the initial probe/camera observations
(18 ticks); its body position and velocity remained identical. A new test-only
subclass freezes the fixture until alignment and records after physics. Run it
unchanged against baseline and successor for all 1260 original input ticks.
The small stale R7 footer binding is also corrected to the current identity.

Bounded repair 2 initializes the test camera transform to identity before its
canonical snap. Godot look_at otherwise preserves tiny basis-scale residue
from ready-time alignment; 19 early camera-basis values differed by one ULP.
No camera or movement implementation changes. The resulting 1260-tick trace
is byte-identical between the same-engine baseline and successor. This final
round also extends retaining-face bands along all contour segments (including
short curved segments and the outer wall), with seams sampled by arc length.
The earlier per-edge length filter omitted most curved-wall detail. This is
presentation only; the original outer boundary collision remains unchanged.
