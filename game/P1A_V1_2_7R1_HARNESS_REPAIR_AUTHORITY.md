# District Zero P1A v1.2.7R1 — authorized harness/evidence repair overlay

**Status:** `READY FOR CODEX — v1.2.7R1 PREFLIGHT REQUIRED`

This document is a test-only successor overlay on the unchanged v1.2.7 bounded-presentation-calibration authority. It exists because the checksum-clean v1.2.7 runner was shown to be contractually unrunnable. The project owner directly authorized the smallest coherent repair of those demonstrated defects.

## Immutable scope

The v1.2.7 candidate domains, quantization, launch cap, metrics, thresholds, ray baseline, deterministic selection rule, presentation targets, v1.2.6 baseline, v1.2.5 capture/replay semantics, and v1.2.3 world authority remain unchanged. Movement, tuning, camera, geometry, collision, routes, terrain, masks, HOP, lighting, shadows, background, tonemapping, normals, other materials, Human World Gate authority, and P1B are frozen.

Only `OUTER_CLOSURE_MASK` and `CORE_WALL` may receive values selected by the existing bounded calibration. R1 does not preselect either value.

## Six authorized repairs

1. Every disposable runnable project is imported and parse-validated with exact Godot before execution. Its generated global script-class cache must resolve the ten registered classes to their exact same-entry resource paths. The byte-preserved `.gd` builder fixture is test data, duplicates `class_name P1AWorldBuilder`, and is therefore excluded only from runnable copies; the authoritative fixture remains in the source and supplies canonical/C1 baseline bytes.
2. Materialization is derived canonically from the frozen v1.2.6 builder, the frozen registry, and strictly validated parameter values. The final verifier requires exact expected bytes and matching source hashes. Low-emission fallback code remains normally shaded and is generated at valid GDScript indentation.
3. Candidate evidence is executed only when the process/result contract, four `1280×720` RGB8 PNGs, 21 metric checks, 14 ray records, hashes, logs, import binding, and material-source identity are complete. Missing or malformed evidence is `BLOCKED/NOT TESTABLE — HARNESS`, never `NO_PRESENTATION_ONLY_INTERVAL`.
4. Every reached blocker, registered negative result, or success is routed through a common terminal evidence finalizer before disposable cleanup.
5. Every delivered ZIP has complete inventory coverage, CRC/duplicate/path checks, safe fresh extraction, and full source-tree byte comparison. Packaging failure cannot be reported as readiness.
6. A prepared session is imported and parsed before readiness; its result is written before its final inventory. Because `.godot` is generated state and is excluded from delivery, the session-bound launcher repeats exact import/parse/class validation on the fresh extraction before creating its single-use marker.

## Mandatory preflight-only gate

Before any candidate render, the R1 runner must execute representative baseline, identity, closure-only, wall-only, emission, and combined materializations; exact-engine import/parse checks; static verifier behavior; synthetic blocker/failure/success package finalization; source-tree round trips; and prepared-session fresh-extraction re-import. Candidate render count must remain zero.

Full calibration may begin only when that preflight produces `PASS` evidence bound to the current `PACKET_SHA256SUMS.txt` identity and a fresh read-only review confirms the six repairs and frozen boundary.

## Terminal boundary

A fully executed but ineligible bounded domain may terminate `NO_PRESENTATION_ONLY_INTERVAL`. Incomplete execution cannot. Stop and package truthful evidence on exact-engine, identity, harness, preservation, final visual/ray, C1, launcher-smoke, or prepared-session failure. Even after complete automation, do not conduct the Human World Gate or enter P1B.
